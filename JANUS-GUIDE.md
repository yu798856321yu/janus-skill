# Janus -全栈自适应研发工作流

---

## 目录

1. 从 Prompt 到 Skill：AI 编程是怎样发展到今天的
2. 为什么普通 AI Coding 在存量系统中容易失控
3. Janus 的定位：规范关键边界，同时保留模型能力
4. 三类工程思想：分别解决什么问题
5. 两层控制框架：存储五元与运行环境三维
6. 六阶段流水线：从一句需求到可审计交付
7. 三车道与五道门：怎样兼顾效率和风险
8. 可靠性机制：如何防止假绿、盲改和越界
9. 四类典型场景
10. 与普通 AI Coding、重型 Skill 的差异
11. 当前能力边界、试点指标与演进方向

---

## 一、从 Prompt 到 Skill：AI 编程是怎样发展到今天的

### 1. 第一阶段：网页对话，AI 主要负责“给建议”

ChatGPT 刚出现时，典型使用方式是：工程师把需求、代码片段或报错复制到网页中，模型给出建议，再由工程师手动复制回本地开发环境。

这一阶段的价值是降低了知识查询和代码编写门槛，但存在三个明显限制：

- 模型看不到完整的本地目录、依赖版本和真实配置；
- 多文件修改依赖人工搬运，容易遗漏；
- 模型不能直接编译、运行和观察结果，结论往往停留在“看起来可行”。

### 2. 第二阶段：使用全局 Prompt 统一基本行为

为了减少重复说明，团队开始使用 System Prompt、Custom Instructions、`AGENTS.md` 等全局规则，统一代码风格、安全要求和沟通方式。

Prompt 并没有过时。它始终是用户表达业务目标、约束条件和临时意图的主要方式。问题在于，全局 Prompt 更适合描述跨任务都成立的基本规范，不适合承载一整套随研发阶段变化的复杂流程。

例如：发现阶段强调只读调查；设计阶段强调契约和范围；实施阶段才允许修改限定文件；验证阶段需要保护断言，不能为了通过而降低标准。

如果把这些规则全部长期放进全局 Prompt，会增加上下文负担，也容易让简单任务承担不必要的流程成本。

### 3. 第三阶段：工具调用让 AI 可以操作本地工程

AI 能够读取本地文件，并不是云端模型直接访问了电脑硬盘，而是通过“模型决策 + 本地宿主执行”的方式完成：

```text
用户提出需求
    ↓
模型判断需要读取文件、搜索代码或运行命令
    ↓
模型发出结构化工具调用请求
    ↓
本地宿主程序执行文件或终端操作
    ↓
真实结果回传给模型
    ↓
模型根据结果继续分析、生成补丁并验证
```

工具调用使 AI 从“代码建议者”变成了“可以参与真实工程操作的研发智能体”。能力提高的同时，风险也从“建议可能不准确”升级为“可能实际改错文件、影响环境或产生副作用”。

### 4. 第四阶段：Skill 将专业 Prompt 按需装载

Skill 的本质仍然是 Prompt。它不是用来取代用户 Prompt，而是把某一类任务需要的专业知识、流程和边界整理成可复用的结构化指令，并在合适时加载。

| 组成 | 主要作用 |
|---|---|
| 用户 Prompt | 说明这次要解决什么业务问题 |
| 全局 Prompt | 规定所有任务都应遵守的基本行为 |
| Skill | 在特定任务中按需补充专业工作方法和约束 |

因此，Skill 的关键价值不只是“写得更长”，而是**按需加载**。只有任务真正需要时才引入专业流程，普通问答和轻量代码任务保持轻量。

### 5. 模型能力提高后，Skill 也需要变轻、变准

早期模型推理能力较弱，逐步指令、固定模板和强制 TDD 能改善输出稳定性。随着模型的代码理解和工具使用能力提高，如果仍对所有任务采用同一套重流程，可能产生新的问题：

- 小任务也需要大量前置文档和步骤；
- 大量固定指令占用注意力，削弱模型对真实代码的理解；
- 在缺少自动化测试的老系统中，固定流程可能无法执行；
- 规则关注“是否走完步骤”，反而弱化“结果是否真的正确”。

Janus 的设计出发点由此形成：**把严格约束放在风险、权限、契约和验证上，把具体实现方式留给模型依据代码事实判断。**

---

## 二、为什么普通 AI Coding 在存量系统中容易失控

新建 Demo 和企业存量系统的差别，不只是代码量不同。存量系统往往同时存在历史约定、共享数据、隐含依赖、未提交修改、老旧技术栈和不完整测试。很多团队兴冲冲引入 AI，跑了一周 Demo 惊为天人，但一进真实生产项目，事故接二连三：

```text
┌───────────────────────────────┐         ┌───────────────────────────────┐
│     小 Demo (Greenfield)      │         │   企业存量系统 (Brownfield)   │
├───────────────────────────────┤         ├───────────────────────────────┤
│ • 没有历史包袱，代码干净     │   VS         │ • 十年历史债务，分层五花八门   │
│ • 单一技术栈，开箱即跑        │             │ • 混合技术栈 				  │
│ • 挂了无所谓，刷新重来        │             │ • 动了核心表，生产线直接停摆   │
│ • 没单测也能靠肉眼看          │             │ • 接口字段错一个，下游全瘫痪   │
└───────────────────────────────┘         └───────────────────────────────┘
```



直接使用 AI Coding 时，常见问题可以归纳为七类：

1. **事实不足就开始修改**：只看局部代码，没有先确认真实入口、调用链和历史约定。
2. **修改范围扩散**：顺手格式化、重构或覆盖用户已有改动。
3. **目标和参考混淆**：把只用于参考的项目误当成实际修改目标。
4. **前后端契约漂移**：字段名、类型、可空性、状态码和默认值各自推测。
5. **过度设计**：小需求被扩展成新的层、接口、工厂或依赖。
6. **错误方向持续试错**：没有稳定的问题标识和停止条件，越修影响面越大。
7. **验证假阳性**：命令成功被误认为业务正确，或通过修改测试来降低验收标准。

这些问题的共同根因是：模型可以快速生成代码，但普通对话并没有天然提供完整的**边界管理、风险分流、证据追踪和停止机制**。

---

## 三、Janus 的定位：规范关键边界，同时保留模型能力

Janus 不是一个新的编程模型，也不是替代 IDE 的工具。它是一套研发工作流 Skill。

它主要做两件事：

- **用三车道决定流程有多重**：低风险任务走 R0，常规功能走 R1，高风险变更走 R2；
- **用共同底线控制所有车道**：保护工作区、限制写入范围、冻结必要契约、要求真实证据、失败及时熔断。

可以将其核心概括为：

> 按任务风险分配流程成本；对实现过程保持弹性，对安全边界和交付证据保持严格。**做“防撞护栏与刹车制动”，不做“机械操作手册”。**
>
> **管什么**：管授权边界、管目录范围、管契约冻结、管真实证据、管故障熔断。
>
> **不管什么**：不教模型怎么写 `for` 循环，不教模型怎么写具体算法，不限制模型充分发挥其高智商推理。
>
> **核心理念**：小活极速直通，大活层层把关；把确定性留给关键资产，绝不反噬模型。

Janus 并非“只重安全”，也并非“只重多车道”。多车道解决效率问题，控制面解决越界问题，验证体系解决可信度问题，三者共同构成完整方案。

---

## 四、三类工程思想：分别解决什么问题

![三大工程思想与 Janus 的融合关系](X:/MES/code_skill-main/fullstack-development/diagram-three-philosophies.png)

> 图中三条输入不是简单拼接。Janus 分别吸收“验证可信”“契约收敛”“实现克制”三类思想，再通过风险分流和控制面把它们组合为一条存量研发流程。可打开[交互版图示](X:/MES/code_skill-main/fullstack-development/diagram-three-philosophies.html)查看。

### 1. Superpowers：解决“未经验证就声称完成”

Superpowers 强调测试驱动、红绿循环和完成前验证。它针对的是 AI Coding 中非常典型的假阳性：模型根据代码外观判断“应该可以”，却没有拿到当次运行证据。

Janus 吸收的重点包括：

- Bug 修复尽可能先观察旧代码的失败，再用同一验证方式观察修改后的成功；
- 交付只能引用当次实际执行得到的新鲜证据；
- 在完成前进行对抗式检查，主动寻找边界、异常和遗漏。

Janus 没有照搬“所有任务一律走完整 TDD”。对于没有单元测试基础的存量工程，它允许根据 Tier A/B/C 选择自动化测试、脚本探针、接口实测、浏览器和日志证据。重点是证明具体契约，而不是完成固定仪式。

### 2. Matt Skills：解决“方案发散和契约靠猜”

![Skills](https://camo.githubusercontent.com/f1dfca7a2d5f6ac1363e06d2d9ddf9504c1afc2b88f8e53483004ebd432de6e0/68747470733a2f2f7265732e636c6f7564696e6172792e636f6d2f746f74616c2d747970657363726970742f696d6167652f75706c6f61642f76313737373338323237372f736b696c6c2d7265706f2d6c696768745f32782e706e67)

Matt Pocock 的 Skills 强调共享语言、清晰类型、深模块和对决策边界的控制。它所针对的典型问题是：模型在信息不足时不断发散，前端、后端和业务各自使用不同理解。

Janus 吸收并综合提炼出：

- **契约优先**：跨端开发先明确字段、类型、必填、可空、默认值和状态码；
- **决策前沿剪枝**：技术上已有代码事实可判断的事项由模型自行收敛，只有会改变业务结果的分歧才请用户决定；
- **共享语言**：需求、设计、实现和验证使用同一组术语和编号。

这样做的意义是，把“联调时才发现理解不同”提前到编码前解决，同时减少无效的开放式提问。

### 3. Ponytail：解决“AI 生成代码容易过度设计”

![image-20260908175005847](C:\Users\JL15329\AppData\Roaming\Typora\typora-user-images\image-20260908175005847.png)

Ponytail 强调 YAGNI、深模块和减少无价值抽象。它针对的是生成式开发常见的复杂度膨胀：为了显得完整，模型容易新增不必要的层、包装类和依赖。

Janus 吸收的重点包括：

- 先复用现有实现和项目惯例，再考虑引入新结构；
- 优先使用标准库、框架原生能力和既有依赖；
- 模块对外接口尽量简单，复杂性收敛在内部；
- 必要的临时妥协使用 `// ponytail:` 显式记录原因、适用上限和后续重构条件。

这使技术债务不再隐藏在代码中，也避免为了一个局部需求制造长期维护负担。

### 4. 三类思想如何形成完整闭环

```text
Matt：先把“要做什么”说清楚
             ↓
Ponytail：用尽量小而深的实现完成它
             ↓
Superpowers：用新鲜证据证明结果满足要求
             ↓
Janus 控制面：确保全过程没有越界，并按风险决定流程深度
```

---

## 五、两层控制框架：存储五元与运行环境三维



​									**在模型执行任何写操作之前，先从物理和逻辑层面划出边界。**

![存储空间五元与运行环境三维控制网格](X:/MES/code_skill-main/fullstack-development/diagram-storage-grid.png)

> 这张图表达两类不同风险：上半部分回答“文件可以写到哪里”，下半部分回答“程序运行后可以影响什么”。两者缺一不可。可打开[交互版图示](X:/MES/code_skill-main/fullstack-development/diagram-storage-grid.html)查看。

### 1. 存储空间五元：把所有路径先分角色

| 路径角色 | 白话解释 | 默认规则 |
|---|---|---|
| `source_targets` | 本次明确要修改的源码文件 | 满足当前档位解锁条件后才可写 |
| `artifact_targets` | 需求、设计、状态和验收报告 | 只允许写入明确列出的产物文件 |
| `scratch_roots` | 构建、日志和中间文件的临时区域 | 位于源码工作区之外，按本次 run 隔离 |
| `reference_roots` | 参考工程、附件、样例和第三方代码 | 只读，不得执行或修改 |
| `forbidden_roots` | 生产路径、系统目录和敏感配置 | 禁止写入和执行，敏感内容还可能禁止读取 |

五元模型的意义是把“用户让我改这个项目”转换为可以核对的具体范围。未列入可写角色的路径默认不写；如果路径角色重叠，采用限制更严的一类。

### 2. 运行环境三维：控制代码运行后的副作用

| 控制维度 | 关注对象 | 典型控制方式 |
|---|---|---|
| `data_scope` | 数据库、Schema、租户和测试数据 | 使用隔离测试范围；当前 L0 不直接写共享库 |
| `runtime_scope` | 服务、端口、进程和系统配置 | 使用本地受控子进程和临时端口，不改全局服务 |
| `external_effects` | MQ、缓存、短信、支付和出站网络 | 在系统接缝处 Mock 或使用专用隔离实例 |

只限制文件不够。例如，测试命令本身没有修改源码，却可能连接共享数据库、发送真实消息或触发自动迁移。因此 Janus 将“磁盘写入”和“运行副作用”分开建模。

### 3. 工作区基线：区分用户原有修改和本次增量

任务开始前建立 Baseline Manifest，记录工作区文件状态；任务结束后重新扫描并比较差异：

```text
最终工作区变化
  - 任务开始前已有变化
  = 本次任务产生的真实增量
```

Git 仓库会补充分支、HEAD、暂存区和未跟踪文件信息；非 Git 目录同样使用文件清单和哈希完成增量核对。这样可以发现白名单外漂移，同时避免用全局 reset 覆盖用户原有工作。

### 4. L0、L1、L2：必须诚实区分规则和物理能力

| 防御层级 | 含义 | 当前状态 |
|---|---|---|
| L0 | 由 Prompt/Skill 约束模型主动遵守规则 | 当前已实现 |
| L1 | 工具执行前由外部 Hook 校验路径、命令和参数 | Roadmap，尚未实现 |
| L2 | 通过容器、ACL、数据库权限和网络策略物理隔离 | Roadmap，尚未实现 |

因此，当前 Janus 可以提升模型遵守规范的概率和过程可见性，但不能把 L0 描述成不可绕过的物理安全边界。

当前 L0 永久不由 Agent 亲自执行三类操作：生产部署、生产数据修改、共享数据库 DDL。即使用户要求，也只能生成离线脚本、候选包和人工交接说明。

---

## 六、六阶段流水线：从一句需求到可审计交付

![Janus 六阶段端到端研发流程](X:/MES/code_skill-main/fullstack-development/fullstack-flow-diagram.png)

> 图的横向分支表示三种风险车道，纵向主干表示六个共同阶段。R0、R1、R2 都要完成发现、定档、设计、实施、验证和交付，但执行深度不同；只有 R2 经过五道人工确认门。可打开[交互版流程图](X:/MES/code_skill-main/fullstack-development/fullstack-flow-diagram.html)查看。

### P1：发现（Discovery）

- **输入**：用户需求、目标工作区、参考附件和当前代码现场。
 - **主要动作**：确认目标与参考路径；建立工作区基线；优先读取目标项目根下的 `docs/tech-stack.md`（按内置 `references/profile-tech-stack.md` 模板配置的技术栈与构建命令）并遵循现有 `AGENTS.md`；扫描构建入口、路由与契约、ORM/迁移、权限以及部署/共享配置；判断验证条件属于 Tier A、B 还是 C。
- **输出**：事实摘要、初始影响面、路径角色、已有修改清单和可测试性结论。
- **为什么先做**：存量系统最危险的错误通常不是“代码不会写”，而是“一开始理解错了工程和影响面”。
- **下一步**：事实足够后进入 P2；扫描不完整或出现高危信号时，扩大调查或升档。

### P2：定档与范围（Triage）

- **输入**：P1 的事实、影响面和风险信号。
 - **主要动作**：依据接口影响（I）、数据影响（D）、契约影响（C）和回滚难度（R）四轴事实打分，选定 R0、R1 或 R2；用一句范围公式固定“在什么现有功能中，把什么旧行为改成什么新行为，影响哪些端，不改什么”；在首次代码实施前，显式回显包含四轴评分事实、技术画像路径与确认要求的结构化定档卡，严禁静默定档。
 - **输出**：结构化定档卡（Risk Scorecard）、范围边界、排除项和升级理由。
- **为什么要做**：没有明确范围，模型容易把局部修复扩展成重构；没有风险分流，小任务会过重，高风险任务又可能过轻。
- **下一步**：R0/R1 进入相应轻量设计；R2 先提交 Gate 1。

### P3：设计与契约收敛（Design）

- **输入**：已确认的范围、现有代码惯例和业务分歧。
- **主要动作**：确定最小修改方案；涉及跨端时冻结字段级契约；识别真正需要用户裁定的业务选择；应用极简阶梯和深模块原则。
- **输出**：最小修改集、契约 Diff、必要的架构或详细设计，以及已收敛的决策。
- **为什么要做**：把代价最低的“理解冲突”留在编码前解决，避免实现完成后再因字段和业务规则不一致返工。
- **下一步**：R0/R1 在没有待决业务问题时进入实施；R2 依次通过 Gate 2 和 Gate 3。

### P4：实施（Implement）

- **输入**：冻结范围、契约、写集和实施计划。
- **主要动作**：沿真实数据流做垂直切片，例如存储/业务逻辑 → API → 请求层 → UI；尽量一次打通一个完整能力；失败由 3/5 熔断机制追踪。
- **输出**：限定白名单内的代码变更、必要测试或探针，以及尝试记录。
- **为什么要做**：垂直切片能更早暴露跨层契约问题，也比按“先写完全部后端、再写全部前端”更容易形成可验证增量。
- **下一步**：最小切片完成后立即进入 P5 验证；验证失败则按故障分类决定修复、暂停或请示。

### P5：验证（Verify）

- **输入**：候选代码、冻结契约和验收项 A<n>。
- **主要动作**：从低成本到高成本执行构建、定向测试、接口调用、浏览器核验和异常场景；记录命令、环境、输出、时间及候选代码哈希。
- **输出**：Acceptance、Evidence、Trace 三张表，以及每项验收结果和证据有效性。
- **为什么要做**：`exit 0` 只能说明命令成功退出，HTTP 200 只能说明一种传输状态；它们必须与具体业务契约共同判断，才能证明功能正确。
- **下一步**：R0/R1 在所有必须项 `PASS + VALID` 后直接进入 P6；R2 冻结候选包后提交 Gate 5。

### P6：交付（Delivery）

- **输入**：通过验证的候选代码、证据集合、工作区增量和债务标记。
- **主要动作**：核对无白名单外漂移；汇总关键改动、契约变化、验证结论、未验证项和 `// ponytail:` 债务；输出三项聚合裁定。
- **输出**：可供接手人复查的交付报告，而不只是“已经完成”的口头结论。
- **为什么要做**：研发结果需要可追溯。后续人员应能回答“改了什么、为什么正确、哪些尚未证明、是否影响原现场”。
- **结束条件**：交付状态必须明确为完全交付、带例外的部分交付或阻塞，不能模糊收尾。

---

## 七、三车道与五道门：怎样兼顾效率和风险

### 1. 三种风险车道

| 车道 | 适用任务 | 流程特点 | 交付方式 |
|---|---|---|---|
| R0 快速档 | 单文件、单点、低影响微调 | 最小事实确认、最小写集、最小验证闭环 | P5 通过后直接交付，无 Gate |
| R1 常规档 | 单模块 CRUD、常规前后端功能 | 契约冻结、决策剪枝、垂直切片 | 必须项验证通过后直接交付，无 Gate 5 |
| R2 严格档 | 跨业务域、新表、复杂迁移、核心鉴权 | 完整设计产物、写集审批和候选包冻结 | 依次经过 Gate 1～5 |

风险档位不是按“改动行数”简单判断。改一行公共鉴权代码可能是 R2，改多个隔离页面也可能仍是 R1。关键是影响面、可逆性、数据风险和不确定性。

### 2. R2 的五道确认门

```text
P1 发现 → P2 定档
              ↓
Gate 1：确认需求和业务边界
              ↓
P3 设计 → Gate 2：确认增量架构
        → Gate 3：确认详细设计和契约
              ↓
P4 前 → Gate 4：确认实施计划和源码写集
              ↓
P4 实施 → P5 验证 → 冻结候选包
                           ↓
                    Gate 5：验收裁决
                           ↓
                        P6 交付
```

每道 Gate 都批准一个带指纹的具体对象，而不是笼统批准“继续”。上游需求、架构、契约、实施计划或候选代码发生变化时，下游审批和证据需要相应失效或重做。

需要特别说明：**Gate 5 表示研发候选结果获得验收，不等于允许部署到生产。**

---

## 八、可靠性机制：如何防止假绿、盲改和越界

### 1. A/V 多对多追踪：把“要求”和“证据”分开

Janus 使用三张表：

- **Acceptance Ledger**：列出 A1、A2……每条业务验收断言及来源；
- **Evidence Ledger**：列出 V1、V2……每次真实执行的命令、环境、输出和代码哈希；
- **Trace Matrix**：说明每条 A 由哪些 V 证明，并给出 Outcome 与 Validity。

一条验收项可能需要多份证据，例如“订单创建成功”同时需要接口报文和数据库只读查询；一份端到端证据也可能覆盖多条验收项。因此 A/V 采用多对多关系，而不是机械地一一对应。

### 2. 证据必须服从契约，而不是固定数字

以下内容都不能单独证明业务正确：命令 `exit 0`、HTTP 200、页面能打开、Console 没有 Error、文件哈希稳定。

它们只是证据的一部分。真正的判断标准来自用户需求、冻结契约和验收场景。例如创建操作可能期望 201，幂等删除可能期望 204，查询没有数据时空列表可能是正确结果。

### 3. 证据与候选代码绑定

验证后如果又修改了源码、依赖、配置或测试断言，旧证据会变成 `STALE`，必须重新执行。这样可以避免“测试通过后又改了代码，仍沿用旧结果”的假交付。

### 4. 调试熔断：在错误方向扩大前停下来

![因果调试循环与熔断机制](X:/MES/code_skill-main/fullstack-development/diagram-debug-circuit-breaker.png)

> 图中内环表示一次“提出假说—修改—验证”；外层计数器限制同一策略和当前排查轮次的失败次数。可打开[交互版图示](X:/MES/code_skill-main/fullstack-development/diagram-debug-circuit-breaker.html)查看。

 - **同策略硬熔断 (`cb_same_strategy_failures: 3`)**：同一排查策略连续失败达到常量阈值（默认 3 次），强制进入 `REASSESSMENT_REQUIRED`，停止该错误方向并重新审视根因，证伪前不允许同向重试；
 - **累积故障熔断 (`cb_lifetime_failures: 5`)**：当前任务累积有效失败达到常量上限（默认 5 次），强制进入 `BLOCKED_USER_DECISION`，保全排查日志与断言现场，由用户决定后续方向；
 - **解耦与事件溯源**：熔断参数由控制面常量集中管理，不再硬编码死锁；改名、升档、恢复均不重置累积 lifetime，只有用户确认新方向并递增 epoch 才能恢复重试。

这套规则不是说“第五次一定不可能成功”，而是限制 AI 在影响面不断扩大的情况下自行无限试错，避免越修越烂。

### 5. 失败分类：不是所有红灯都算代码回归

| 类型 | 含义 | 处理 |
|---|---|---|
| `CODE_REGRESSION` | 本次候选修改导致失败 | 计入修复尝试和相应熔断 |
| `BASELINE` | 修改前已经存在的问题 | 记录但不归咎于本次变更 |
| `ENVIRONMENT` / `DEPENDENCY` | 端口、账号、网络或服务缺失 | 暂停并说明阻塞条件 |
| `TEST_DEFECT` | 测试与独立规格不一致 | 有证据且不削弱契约时才允许修正 |
| `TRANSIENT` | 偶发文件锁、短暂超时 | 仅允许一次有记录的安全重试 |
| `UNKNOWN` | 尚无法归因 | 先暂停分类，不继续盲改 |

### 6. 无法稳定复现时，允许诚实的部分结论

线上偶发 Bug 可能依赖生产数据、特定账号或时序条件，本地无法稳定复现。此时不能伪造红灯，也不能声称完整验证。Janus 允许记录排查过程和缺失条件，并标记为 `PARTIAL_UNVERIFIED` 或 `BLOCKED`。

### 7. 数据库恢复不是一个“回滚”就能概括

Janus 区分 Schema Rollback、Data Recovery、Business Compensation 和 Forward-fix。当前 L0 下，Agent 不直接在共享数据库执行 DDL 或数据写入。可以准备迁移脚本和恢复方案，交由 DBA 或受控系统审查执行。

---

## 九、四类典型场景

### 场景一：修改一个页面文案或样式

- **定档**：通常为 R0。
- **做法**：确认真实目标文件，锁定 1～2 个 `source_targets`，按现有样式修改并做最小界面验证。
- **避免的问题**：不为小任务创建完整 PRD、架构文档和五道审批，也不顺手重构周边组件。

### 场景二：新增一个前后端业务字段

- **定档**：通常为 R1；若涉及核心公共契约或复杂数据迁移则升 R2。
- **做法**：先冻结字段名、前后端类型、必填、可空、默认值和状态码，再沿垂直切片完成实体、服务、API、请求层和界面。
- **避免的问题**：前后端各自猜字段，直到联调才发现不一致。

### 场景三：数据库结构变更或核心权限改造

- **定档**：R2。
- **做法**：经过需求、架构、详细契约和写集审批；准备迁移与恢复交接资产；验证候选包后再做 Gate 5 裁决。
- **避免的问题**：AI 直接连接共享库执行 DDL，或把“有 Down 脚本”误认为数据一定可恢复。

### 场景四：线上偶发问题无法本地复现

- **定档**：根据影响面选择 R1 或 R2。
- **做法**：保留稳定的 issue 标识，记录可证伪假说和排查证据；缺少关键条件时停止扩大修改，并如实标记未验证范围。
- **避免的问题**：为了交付而伪造红绿闭环，或在错误方向无限试错。

---

## 十、与普通 AI Coding、重型 Skill 的差异

| 维度 | 普通 AI Coding | 一刀切重型 Skill | Janus |
|---|---|---|---|
| 调用方式 | 主要依赖临时对话 | 容易在大量任务中强制介入 | 当前设计为用户显式调用 |
| 简单任务 | 快，但容易漏边界 | 流程成本偏高 | R0 保留最小保护后快速完成 |
| 常规跨端功能 | 容易发生契约漂移 | 流程完整但可能偏固定 | R1 重点冻结契约和垂直切片 |
| 高风险任务 | 依赖模型临场判断 | 通常强调通用测试流程 | R2 增加路径、权限和人工 Gate |
| 老系统测试 | 容易只做表面验证 | 缺单测时可能卡住 | Tier A/B/C 选择现实可行证据 |
| 调试失败 | 容易连续盲改 | 可能有流程但缺稳定计数 | issue、strategy、epoch 三层追踪 |
| 交付结论 | 常是自然语言总结 | 通常强调测试完成 | A/V 追踪、证据有效性和例外披露 |
| 安全强度 | 通常没有明确分层 | 容易把规则写成强保证 | 明确区分 L0、L1、L2 |

这张表并不表示某一种方式在所有场景都更好。普通对话适合咨询和局部建议；重型流程适合测试基础好、流程高度标准化的团队；Janus 重点服务于风险差异大、历史包袱重、验证条件不统一的企业存量工程。

---

## 十一、当前能力边界、试点指标与演进方向

### 1. 当前可交付能力

当前 Janus 已能在 L0 政策层提供：

- 显式触发，避免无差别介入普通任务；
- R0/R1/R2 风险分流；
- 存储五元、运行环境三维和工作区基线规则；
- 契约先行、垂直切片和 3/5 熔断；
- A/V 三表与证据有效性要求；
- 高危动作的离线交接模式。

这些能力能够提高过程一致性、暴露剩余风险，并为审查和复盘提供结构化信息。

### 2. 当前不能宣称的能力

以下能力尚未由 L1/L2 基础设施实现，不能作为当前保证：

- 工具调用前不可绕过的路径物理拦截；
- 容器、ACL、数据库账号和网络出口的强隔离；
- 不可由 Agent 修改或伪造的审批记录；
- 自动隔离数据库、MQ、缓存和真实下游；
- 仅凭静态 validator 证明运行时行为可靠。

因此更准确的说法是：Janus 当前提供**可执行的研发治理规则和审计框架**，而不是完整的安全沙箱。

### 3. 建议的试点指标

不要预先承诺“减少 80%”或“零事故”。先选择一批有代表性的 R0、R1、R2 任务建立基线，再观察：

| 指标 | 建议定义 |
|---|---|
| 非预期文件变更率 | 白名单外新增、删除或修改的文件数 / 任务数 |
| 契约返工率 | 因字段、类型、可空性或状态码不一致导致的返工任务占比 |
| 验证覆盖完整度 | 有有效证据闭合的必须验收项 / 全部必须验收项 |
| 假阳性交付率 | 宣称完成后被证实未满足验收契约的任务占比 |
| 无效重试率 | 未形成新证据或新假说的重复尝试占比 |
| 周期变化 | 同类型任务使用前后的中位交付时长 |

建议按任务档位分别统计，避免 R0 和 R2 混在一起造成结论失真。

### 4. 演进路线

```text
当前 L0：完善规则、模板、状态和静态校验
    ↓
未来 L1：增加工具前置拦截、路径规范化和可信状态存储
    ↓
未来 L2：增加容器、权限、临时数据库和网络副作用隔离
```

L0 的价值在于先把正确的工作方式固化并接受真实任务检验；L1/L2 的价值在于把关键约束从“要求模型遵守”逐步升级为“执行环境能够强制”。

### 5. 如何调用

Janus 采用严格的显式触发策略（`allow_implicit_invocation: false`）。用户在对话中明确输入 `$janus` 或明确指令“按 Janus 流程”时触发启用，不会在普通聊天中无差别激活。

普通技术问答、语法咨询、单段代码示例和没有要求本地落盘验证的轻量任务，不应自动套用完整流程。项目如果需要定制专属构建命令或禁止改动的目录，可将技能包内的 `references/profile-tech-stack.md` 模板复制为目标工程根目录下的 `docs/tech-stack.md` 填写，下次对话调用时热插拔即时生效，无需重新安装技能。

一个典型输入可以是：

```text
$janus 完成订单列表的状态筛选功能。
目标工程：X:\MES\order-system
参考工程：X:\MES\legacy-order（只读）
要求：前后端一起修改，真实验证后交付，不允许操作共享数据库。
```

### 6. 工程实施与自测试质量门

为了确保技能自身不成为不稳定因素，Janus 建立了工业级自我校验体系：
- **14 文件严格发行闭包**：通过 `release-manifest.json` 锁定全部载荷与清单指纹（`payload_digest` 与 `package_digest`），确保运行代码与已审计版本 100% 字节一致；
- **104 项全量自动化自测试 (`-SelfTest`)**：覆盖 frontmatter、受限 YAML 解析、编码策略、路径穿越安全、NTFS 特性、故障注入模拟（MidCopy、PostCopy、BeforeRename 原子回滚）以及 `janus-contract-v1` 契约摘要算法的双黄金向量测试；
- **内置显式清单刷新工具 (`-RefreshManifest`)**：支持维护者在文档演进后一键安全刷新 SHA-256 清单并自动完成校验闭环；
- **运行环境边界**：校验器与自测套件运行于 Windows 本机固定 NTFS 环境下的 PowerShell 5.1 / 7，技能正文保持轻量提示词规约，不为普通任务引入多余运行时负担。

---

## 结语

Janus 试图解决的不是“怎样让 AI 写出更多代码”，而是“怎样让 AI 在复杂存量工程中，以与风险相匹配的流程完成可验证交付”。

它不替代工程师的业务判断，也不把 Prompt、测试或人工审批变得不重要。它把这些要素组织成一条更清晰的路径：先理解现场，再确定范围；先对齐契约，再实施；用真实证据验证；风险越高，人工控制越强；无法证明的部分如实保留。

对管理者而言，它提供的是过程可见性和风险分层；对研发人员而言，它减少重复沟通和无效返工；对 AI 而言，它明确了哪些地方可以自主推理，哪些边界必须停下来等待确认。
